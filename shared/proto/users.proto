syntax = "proto3";

package users;

service UsersService {
  rpc GetUser (UserId) returns (User);
}

message UserId {
  string userId = 1;
}

message User {
  string userId = 1;
  string name = 2;
  string address = 3;
  string email = 4;
}
