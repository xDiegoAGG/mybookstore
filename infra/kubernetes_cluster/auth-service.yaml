apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: mybookstore
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001
