apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: mybookstore
spec:
  type: ClusterIP
  selector:
    app: catalog-service
  ports:
    - name: http
      port: 3003
      targetPort: 3003
    - name: grpc
      port: 50053
      targetPort: 50053
